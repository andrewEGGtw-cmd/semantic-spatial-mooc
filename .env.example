# 複製本檔為 .env 並填入金鑰；.env 已被 .gitignore 排除，切勿提交。
# 僅 scripts/A3_run_bloom.py（LLM Bloom 標記）需要以下兩把金鑰。

ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxx
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx

# 使用方式（bash）：
#   set -a && source .env && set +a
#   python3 scripts/A3_run_bloom.py --smoke
